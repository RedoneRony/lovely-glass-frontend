import * as React from "react";
import Grid from "@mui/material/Grid";

const DashboardHome = () => {
  return (
    <Grid container spacing={2}>
      <Grid item xs={12} sm={7}>
        <h3> Welcome To Lovely Glass Dashboard</h3>
      </Grid>
    </Grid>
  );
};

export default DashboardHome;
